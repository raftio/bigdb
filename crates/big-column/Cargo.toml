[package]
name = "big-column"
description = "Column segments: values in record order, block-encoded, for the columnar table engines."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
big-btree = { path = "../big-btree", version = "0.1.0" }
big-page = { path = "../big-page", version = "0.1.0" }
big-pager = { path = "../big-pager", version = "0.1.0" }
big-container = { path = "../big-container", version = "0.1.0" }

[dev-dependencies]
proptest = "1"
